[package]
name = "postprocess"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
ahash = "0.8"
regex = "1"

[profile.release]
opt-level = 3
lto = true
