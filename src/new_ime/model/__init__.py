"""CTC-NAT model + presets. To be implemented.

Will host:
    new_ime.model.ctc_nat        — encoder + CTC head
    new_ime.model.refine         — MaskCTC refine decoder
    new_ime.model.presets        — 30m / 41m / 90m configs
"""
